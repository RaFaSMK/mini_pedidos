# --- Configuração do Banco de Dados ---
# Usado pelo Docker Compose para criar o container do Postgres
# e também pela DATABASE_URL abaixo para conexão local.
DB_USER=pedidos
DB_PASSWORD=SUA_SENHA_AQUI
DB_NAME=mini_pedidos
DB_PORT=5432

# --- Configuração da API ---
# Porta que a API vai rodar (local e no Docker)
PORT=3001

# --- String de Conexão (APENAS para dev local) ---
# Usada quando você roda 'npm run dev' (fora do Docker).
# O Docker Compose vai IGNORAR este valor e usar o seu próprio
# (definido no 'environment' do serviço 'backend' no docker-compose.yml).
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}?schema=public"